
# Apply one hot encoding
# from sklearn.preprocessing import OneHotEncoder
# def one_hot_encode(y_original, num_classes):
#     OHC = OneHotEncoder()
#     y_ohc = OHC.fit(np.arange(num_classes).reshape(-1, 1))
#     y_one_hot_encoded = y_ohc.transform(y_original.reshape(-1, 1)).toarray()
#     return y_one_hot_encoded

# print(y_dataset.shape)

# y_dataset = one_hot_encode(y_dataset, n_classes)
# # y_test = one_hot_encode(y_test, n_classes)

# print(y_dataset.shape)
